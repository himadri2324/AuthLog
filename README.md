# AuthLog
This project focuses on analyzing Windows authentication logs using Elastic SIEM to detect repeated failed login attempts. It collects system events, processes them centrally, and identifies suspicious authentication patterns that may indicate brute-force or unauthorized access activity.
